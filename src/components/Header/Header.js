import React from "react";
import "./index.scss";

const Header = (props) => {
  return (
    <div className="header-container">
      <span className="logo"></span>
    </div>
  );
};

export default Header;
